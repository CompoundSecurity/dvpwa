aiohttp-jinja2==1.6                  # upgraded from 1.1.0; supports aiohttp 3.x + jinja2 3.x
aiohttp-session==2.12.1              # upgraded from 2.7.0; supports aiohttp 3.13.x
aiohttp==3.13.4                      # upgraded from 3.5.3; fixes 20+ CVEs (see security report)
aiopg==0.15.0
aioredis==1.3.1                      # no CVEs; note: 1.x API incompatible with Python 3.11 (app code update needed)
aiohappyeyeballs==2.6.1              # new transitive dep via aiohttp 3.13.x
async-timeout==4.0.3                 # upgraded from 3.0.1; via aiohttp, aioredis
aiosignal==1.4.0                     # new transitive dep via aiohttp 3.13.x
attrs==26.1.0                        # upgraded from 18.2.0; via aiohttp
frozenlist==1.8.0                    # new transitive dep via aiohttp 3.13.x
hiredis==3.3.1                       # upgraded from 0.3.1; via aioredis
idna==3.10                           # upgraded from 2.8; fixes CVE-2024-3651 (HIGH)
jinja2==3.1.6                        # upgraded from 2.10; fixes 6 CVEs including sandbox escapes
markupsafe==3.0.3                    # upgraded from 1.1.0; via jinja2 3.x
multidict==6.7.1                     # upgraded from 4.5.2; via aiohttp, yarl
propcache==0.4.1                     # new transitive dep via aiohttp 3.13.x
psycopg2==2.7.6.1                    # via aiopg
pyyaml==6.0.3                        # upgraded from 3.13; fixes CVE-2017-18342, CVE-2020-14343 (CRITICAL)
trafaret-config==2.0.2
trafaret==2.1.1                      # upgraded from 1.2.0; via trafaret-config
yarl==1.23.0                         # upgraded from 1.3.0; via aiohttp
